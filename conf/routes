# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Trae todos los Negocios
GET     /shops/all                 controllers.Application.getShops

#Get nuevo negocio
GET 	/newshop				controllers.Application.newShop

#Agregar negocio
POST	/shops				controllers.Application.addShop

# Precios
GET     /found-prices                   controllers.Application.getFoundPrices

#Get nuevo negocio
GET 	/newfoundprice			controllers.Application.newFoundPrice

#Agregar negocio
POST	/found-prices				controllers.Application.addFoundPrice

# Busca negocios por distintas caracteristicas
GET   /shops         	 controllers.Application.getShopsFor(name:Option[String],location:Option[String],latitude:Option[Double],longitude:Option[Double],address:Option[String])

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
